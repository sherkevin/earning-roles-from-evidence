# Peer-judgment observation smoke v2 — development evidence

2026-09-23. The [prospective v2 amendment](../../../../configs/aamas2027/peer_judgment_smoke_v2.json) fixed two fresh train sibling IDs in order, a named real provider, isolated producer/consumer worlds, call caps, one charged pre-world timeout retry, and a public-read requirement. The run completed both scheduled episodes. Both are `INVALID` before a producer handoff; the first is a transport `UNKNOWN`, and the second exposes a native runtime guard failure. This is observation-controller feasibility evidence only. It supplies no consumer judgment, official task score, role update, or method comparison.

| Fixed task | Real model and world record | End state |
|---|---|---|
| `22cc237_2` | Two producer API attempts, each ending in `TimeoutError` after about 120 s. The declared 2 s retry occurred before any `world.execute`; no response or known token usage. | Transport `UNKNOWN`; zero world executions, proposal, and consumer calls. |
| `3c13f5a_2` | Five successful `qwen3.8-max` responses; four `world.execute` steps. Three `api_docs` calls failed with `PermissionError: Usage of the following function is not allowed: pathlib.Path.mkdir.` A local arithmetic check succeeded. The fifth response tried `dir(apis)`; the controller blocked it before execution with `ValueError: REPL name blocked: dir`. | `INVALID` runtime/guard path; zero successful public task-data reads, proposal, and consumer calls. |

Across the fixed pair, there were **7 real provider attempts, 5 responses, 2 timeout errors, 2 attempts with unknown usage, 4 world executions, 0 public task-data reads, 0 sealed proposals, 0 consumer judgments or actions, and 0 official scores**. The five responses report 7,471 input, 2,785 output, 12,288 cache-read input, and 0 cache-creation input tokens. The two timed-out attempts have unknown usage; zero recorded tokens for them does **not** mean zero billed tokens. Reported producer API time sums to 322.213 s and episode wall times to 337.742 s. USD cost is unknown because a verified price is unavailable. The [processed results](processed_results.json), [raw event logs](episodes/), and [native result records](results.json) retain each attempt and failure.

The live `api_docs` failures correct the earlier CPU guard-fixture interpretation: a fake-requester test of the local gate did not show that AppWorld's native documentation functions work in the guarded REPL. Here the model followed the documented discovery path three times and each call failed on `pathlib.Path.mkdir`; a later inspection attempt was separately rejected by the controller's `safe_code`. Thus a CPU-only guard fixture must not be reported as a successful native documentation preflight. A post-run local invocation, `python3 -m unittest discover -s scripts -p 'test_aamas_peer_judgment_smoke_v2.py' -v`, under system Python 3.9.6 produced five passes and one error: `ModuleNotFoundError: No module named 'appworld'` in the fake-requester guard test. That invocation made no provider call and does not resolve the live guard failure.

Provenance checks used the [source manifest](source_manifest.json), both episode headers, and the [executed runner snapshot](runner_at_execution.py). SHA256 of that snapshot is `83c06393378d88ce0a6953f2d28efb35df7d2b3a09c415eb3132c221fb89f09d`, matching the manifest, both episode `script_sha256` values, and the current source runner at reduction time. The manifest's config SHA256 `1b089c9d0978f44a8dccaac919048ae06aa7a8fe94cc06b6630e1bb90ba77573` matches the source config and both episode headers. [frozen_config.json](frozen_config.json) has the same parsed JSON values but is a reserialized copy with a different byte SHA256, `829032bebc7ce618559a92660bd98f5c9771e9b48faaba11161a9c78f8f8bb3e`; the manifest hash should not be described as the frozen copy's byte hash. The manifest's v1 runner/results hashes match the amendment, and both v2 episode headers repeat the v1 results hash. No runner, config, or original raw record was changed during this reduction.

The reusable result is a concrete protocol defect to address before another observation attempt: the read-only public API documentation path must work under the native guard and be checked in the actual AppWorld world, while preserving its restriction on private files and writes. The current observations cannot tell whether an independent recipient would use, rework, or reject a producer's work, or whether such judgment has predictive value. B5.3/G2 remain open; the v2 failures are not evidence for or against peer-judged role learning.
