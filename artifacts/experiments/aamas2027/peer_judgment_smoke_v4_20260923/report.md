# Peer-judgment observation smoke v4 — development evidence

2026-09-23. The [prospective v4 contract](../../../../configs/aamas2027/peer_judgment_smoke_v4.json) used a compact public AppWorld prompt and reattempted `3c13f5a_3` once after v3 had received no model response and made no world call. This is a **same-task development continuation**, not a new statistical sample. It reached a sealed producer proposal, then stopped on the consumer's first review response before any consumer world action or judgment.

| Phase | Real provider responses | Native world and gate record |
|---|---:|---|
| Producer | 7/7 attempts returned `qwen3.8-max` responses | Six `world.execute` steps generated 33 audited public API calls, including five successful task-data GET observations. Two phone API calls returned errors; no task write occurred. The controller accepted and [sealed a three-row proposal](episodes/3c13f5a_3/proposal.json). |
| Consumer review | 1/1 attempt returned a `qwen3.8-max` response | Its code began with `import apis`. `safe_code` raised `ValueError: Import outside public computation allowlist` before `world.execute`. No review judgment was sealed, and the action phase never began. |

The total is **8 real attempts and 8 responses, 0 provider errors, 6 world steps, 1 controller-valid sealed proposal, 0 consumer world steps, 0 sealed judgments, 0 consumer actions, and 0 official scores**. The provider reported producer usage of 14,669 input, 7,559 output, and 41,472 cache-read input tokens; consumer review usage was 1,409 input and 1,453 output tokens. Combined reported usage is 16,078 input, 9,012 output, 41,472 cache-read input, and 0 cache-creation input tokens. Provider time totaled 251.618 s; episode wall time was 256.874 s. USD cost is unknown. The seventh producer response alone reported 3,298 output tokens despite the requested `max_output_tokens=1024`, so the request cap must not be substituted for observed usage. Per-attempt usage and timings are in the [processed results](processed_results.json) and [raw log](episodes/3c13f5a_3/raw.jsonl).

The proposal passed the controller's schema and minimum-public-read gate, but its citations have a narrower meaning than substantive grounding. Each of the three rows cites steps `[1,3,4,5]`; step 3 was a file-system directory listing and is the qualifying public read. The bill file and roommate contact details were read at step 6, which no row cites. Thus the accepted proposal is a valid **controller artifact**, while the recorded `source_steps` do not establish claim-specific provenance for the recipient and amount. No downstream recipient assessed or used it, and the official evaluator did not run. The consumer's `import apis` failure was a code-allowlist stop, not a rejected judgment artifact; the result records zero consumer schema-validation rejections.

Provenance checks: [runner_at_execution.py](runner_at_execution.py) has SHA256 `067d566558b6fa07b2f91dd0e74a2cbf546bd3a186408b4dda24ad01745432f9`, matching the [source manifest](source_manifest.json), episode header, and current source runner. Source config and archived [frozen_config.json](frozen_config.json) are byte-identical at SHA256 `56db8eb1c30b40b3377b227d91b62c959a7a9c4419aba9524238c6313dde00b0`, matching the manifest and header. The manifest's v3 runner/results hashes match the amendment, and the v3 results hash matches the v4 header. The sealed proposal file matches its logged SHA256 `899b77381ab580733ae5b16430452e09256b43bb38ad27ce8a7e960fef4d8dad`. The compact producer and consumer prompt hashes and character counts are retained in the raw log. No source, config, or raw execution file was changed in this reduction.

The observation shows that the compact-prompt continuation obtained a producer artifact and exposed a consumer code-allowlist boundary. One same-task run cannot attribute the absence of timeouts to prompt size. There is still no actual consumer judgment or use/rework, task outcome, role update, or evidence of method efficacy; B5.3/G2 remain open.
