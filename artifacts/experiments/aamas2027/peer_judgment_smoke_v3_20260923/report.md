# Peer-judgment observation smoke v3 — development evidence

2026-09-23. The [prospective v3 contract](../../../../configs/aamas2027/peer_judgment_smoke_v3.json) fixed one fresh train sibling, `3c13f5a_3`, for a bounded integration attempt after the v2 audit-I/O repair. The real episode ended in transport `UNKNOWN` before the model returned anything. It did not exercise a live producer handoff or consumer judgment.

The producer made two real `qwen3.8-max` API attempts. Attempt 1 timed out after 120.118 s; the declared 2 s retry occurred before any `world.execute`. Attempt 2 timed out after 120.120 s. Both failures are retained in the [raw event log](episodes/3c13f5a_3/raw.jsonl) and [native result](episodes/3c13f5a_3/result.json). There were **2 attempts, 0 responses, 0 world executions, 0 public task-data reads, 0 proposals, 0 consumer calls or judgments, and 0 official scores**. Provider time totaled 240.249 s; episode wall time was 247.161 s. Both attempts have unknown token usage, so the recorded zero response tokens do not imply zero billed tokens. USD cost is unknown. The [processed results](processed_results.json) retain per-attempt times and the zero-stage counts.

The [separate CPU-only native guard fixture](../../../aamas2027/runtime_checks/peer_guard_v3_native_20260923/results.json) passed before the model episode: with AppWorld's native guard enabled and no LLM calls, `api_docs.show_app_descriptions` and `phone.get_current_date_and_time` both succeeded; one was a public task-data read, and both requester events were persisted after the guarded call. Its [initial setup failure](../../../aamas2027/runtime_checks/peer_guard_v3_native_20260923/results_initial_failure.json) is retained: `APPWORLD_ROOT` was omitted, so the task directory resolved under `earning-roles` and the world could not open. The [retry record](../../../aamas2027/runtime_checks/peer_guard_v3_native_20260923/retry_config.json) documents the corrected environment and unchanged fixture SHA256 `52a9dce2b4e792f5ef699c630ba72b855b63afe8301b5326e3cbac8c66b9f36f`. This fixture verifies the repaired native documentation/read path in a CPU run; it is separate from the timed-out real model episode.

SHA256 of [runner_at_execution.py](runner_at_execution.py) is `6813ade1082e0fda02ec2068f01014bf0279fa126dc018496b07010f71ff4004`, matching the [source manifest](source_manifest.json), episode header, and current source runner. Source and archived [frozen_config.json](frozen_config.json) are byte-identical at `e73fd0d8daa8119c89bcc3c1035d3d1ab87a88d2cc8c4f690b80d2b26b14449a`, also matching the manifest and episode header. The manifest's v2 runner and results hashes match the v3 amendment; the v2 results hash matches the episode header. The source AppWorld commit and no-evaluator solver boundary also match the recorded header.

The fixture shows that the repaired read path is usable under the native guard without a model. The real episode gives no evidence about whether a model can make a grounded delivery, whether a recipient would use or rework it, or whether peer judgments help role formation. B5.3/G2 remain open; no efficacy or novelty conclusion follows.
