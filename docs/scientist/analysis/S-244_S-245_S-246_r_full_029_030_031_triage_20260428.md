# S-244 / S-245 / S-246 Triage For R-FULL-029/030/031

Date: 2026-04-28
Author role: scientist

Review artifacts:

- `R-FULL-029` BP-NOVELTY: `artifacts/idea_reviews/reviewer_20260428_1605_29_0c65d2/review.md`
- `R-FULL-030` BP-SOUNDNESS: `artifacts/idea_reviews/reviewer_20260428_2102_30_0c65d2/review.md`
- `R-FULL-031` BP-ETHICS: `artifacts/idea_reviews/reviewer_20260428_2110_31_0c65d2/review.md`

## 1. Shared Read

All three reviews agree with the current scientific direction:

1. The theory/prototype boundary is honest and useful.
2. The best novelty remains in full EDO, while delivered evidence is still restricted.
3. The paper needs direct mechanism metrics, sensitivity evidence, and artifact/reproducibility clarity.
4. Matched external baselines remain a major blocker, but that path is user-gated by `C-027`.

New timing update: `E-055` and `E-056` have now landed after the reviewed PDF snapshot. They do not make the paper positive, but they improve claim hygiene by adding measurable mechanism and sensitivity evidence.

## 2. Accepted Points

### R-FULL-029 BP-NOVELTY

Accepted:

- The paper's novelty remains partly prospective because R1 split, R2 recursive audit, and R3 vector belief are Stage-2.
- Direct organization metrics were missing in the reviewed PDF.
- The paper should avoid reading as if rich emergent specialization has already been shown.

Action:

- Updated `edo_paper.tex` to add bounded mechanism evidence from `E-055` / `E-056`: path length, audit rows, terminal entropy, and memory/tool events are now named, with negative interpretation.

### R-FULL-030 BP-SOUNDNESS

Accepted:

- A full implemented-vs-theoretical component table would help, but page budget is currently too tight for a main-body table.
- The main body should nevertheless make the executed-vs-roadmap boundary even clearer.
- Sensitivity evidence should be added once available.

Action:

- The new mechanism text states that current logs bound rather than strengthen the organization claim.
- `E-056` is used as bounded negative sensitivity evidence: memory/tool switches change traces but are not performance-positive.
- A larger component-status table remains a good appendix / future main-body compression target, not something to force into the current 8-page body without displacing higher-value content.

### R-FULL-031 BP-ETHICS

Accepted:

- The ethics/limitations tone is strong, but artifact auditability needs better support.
- The Responsible NLP checklist is in the appendix, but a visible artifact manifest helps the submission package.

Action:

- Added `docs/paper/artifact_manifest_20260428.md` as a working artifact/provenance manifest covering paper sources, code, seed slices, evidence docs, and known gaps.

## 3. Rejected Or Evidence-Gated Points

- Do not create duplicate paid-baseline tasks: `C-027` already gates canonical paid Wave-1.
- Do not claim EDO memory/tool governance improves accuracy: `E-056` says the opposite.
- Do not claim rich emergent specialization: `E-055` reports terminal-agent entropy is zero in summarized chain runs.
- Do not launch another full review until either `C-026`, `C-027`, or new engineering evidence materially changes the PDF.

## 4. Paper Edit Verification

Main paper build after adding bounded mechanism evidence:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File "scripts/build_paper.ps1" -Clean
```

Result:

```text
[OK] build succeeded
PDF pages including Limitations and References: 10
Main body ends on page 8: COMPLIANT
Overfull hboxes: 0
Underfull hboxes: 32
```

## 5. Status

`S-244`, `S-245`, and `S-246` are complete. Remaining scientist-side frontier:

- `C-027`: user decision on paid canonical Wave-1;
- `C-026`: final Figure 1 / Figure 3 assets;
- no unblocked scientist paper-editing task remains after this triage.
