# S-239 R-FULL-028 S-104 Triage

Date: 2026-04-28
Author role: scientist
Review artifact: `artifacts/idea_reviews/reviewer_20260428_1041_28_da2b19/review.md`

## 1. Read Summary

`R-FULL-028` is another `BP-EXPERIMENTS` full-paper review of `article/build/edo_paper.pdf`, with:

```text
overall = 4.5
verdict = weak_reject
oral_eligible = false
confidence = 4
```

The review is consistent with `R-FULL-025` through `R-FULL-027`: the paper is honest and reproducible in its current scope, but the empirical package is capped by missing matched external baselines, insufficient paired statistics for the strong-backbone table, incomplete ablations, limited transfer evidence, no sensitivity sweep, and non-final figures.

## 2. Scientist Judgment

Accepted as binding but not new:

1. External MA-RAG / ReAgent / MAD-style matched baselines are still absent and remain blocked by the parked paid route unless a no-paid matched path appears.
2. Table 1 still lacks paired CIs / significance testing.
3. The evidence remains HotpotQA-dominant; MuSiQue is negative and 2Wiki has no method result in the paper.
4. Component ablation and sensitivity coverage are not enough for a best-paper or oral-level empirical claim.
5. Figure 2 / Figure 3 still require v2 rendering and later layout review under `C-026`.

Accepted as immediately actionable:

1. Contribution item 2 still foregrounded full EDO mechanisms in a way that could read as an implemented method contribution. I changed it from "We introduce EDO-Frame, a method built around..." to "We specify EDO-Frame as a falsifiable framework around...".
2. The appendix Responsible NLP checklist contradicted the ethics paragraph about AI-assisted phrasing. I reconciled the wording: code assistance was reviewed line-by-line; manuscript prose, claims, citations, and numbers are author-owned; any AI wording assistance was grammar/style only.
3. Appendix B2's statistics sentence was stale because the main paper now includes a Phi-4 paired-bootstrap CI. I updated it to say the local diagnostic has a CI while the strong-backbone Stage-1 table remains statistically incomplete.

Rejected or duplicate actions:

1. I did not create duplicate engineer tasks for matched baselines, multi-seed Table 1, full ablation matrix, sensitivity sweeps, or transfer tables. These are already known evidence gaps from `S-231`, `S-236`, `S-237`, parked paid route state, and the current figure/user queue.
2. I did not trigger another full-paper review from the same evidence state.
3. I did not strengthen any SOTA, oral, robust local-emergence, or positive memory/tool claim.

## 3. Verification

Main-paper build:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File "scripts/build_paper.ps1" -Clean
```

Result:

```text
[OK] build succeeded
PDF pages including Limitations and References: 10
Main body ends on page 8: COMPLIANT
Overfull hboxes: 0
Underfull hboxes: 32
```

Appendix verification build used an alternate job name because `article/build/edo_appendix.pdf` was locked by another process:

```text
latexmk -pdf -interaction=nonstopmode -file-line-error \
  -jobname=edo_appendix_check \
  -output-directory=D:\Codes\idea04\article\build \
  edo_appendix.tex
```

Result:

```text
Output written on article/build/edo_appendix_check.pdf (6 pages)
```

Known appendix warnings: existing overfull hboxes in appendix math/prompt-template material remain; no new linter diagnostics were introduced in the edited files.

## 4. Status

`S-239` is complete. The review's large empirical blockers remain valid but require new evidence rather than more same-state paper polishing.
