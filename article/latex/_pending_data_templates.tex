%% =============================================================
%% _pending_data_templates.tex
%% =============================================================
%%
%% Created: 2026-04-20 (R31 commit, scientist self-prep per user
%% instruction "提交任务后立即做下一步").
%%
%% PURPOSE: Pre-written LaTeX skeletons for the §4.x tables,
%% paragraphs, and Related Work updates that scientist will inline
%% into edo_paper.tex AS SOON AS the corresponding engineer batches
%% (E-017 / E-014 / E-012 / E-016) ack their data. Each block is
%% labelled with: (1) source ticket, (2) which scientist S-XXX it
%% closes, (3) where to insert in edo_paper.tex, (4) the exact
%% placeholders to fill from the engineer's CSV / metrics.json
%% output.
%%
%% This file is NEVER \input{} into edo_paper.tex; it is a holding
%% area only. Inlining is a manual scientist commit (R32+ when data
%% arrives).
%%
%% NOTE: column-spec (\@{}lcccr@{}, etc.) deliberately mirrors the
%% existing Table 1 / Table 2 conventions so visual style matches.
%% =============================================================


%% =============================================================
%% TEMPLATE 1 — S-117 fullval Stage-2 vs Stage-1 paired result
%% Source ticket: E-017 (3-seed × 7405 paired fullval, R21 派工)
%% Closes: S-117 main fullval table + S-115 §1 framing + S-116 §6
%% Conclusion + S-009 §4.3 真数字
%% Insert location: end of §4.3 (after Table 1, before Finding 4 OR
%%   between Finding 4 and §4.4) as a NEW paragraph + table.
%% Section title proposal: §4.3.5 "Stage-2 prototype fullval"
%%   (do NOT make new subsection; integrate as paragraph)
%% Required engineer artifact:
%%   artifacts/round2_gpt41mini_stage2_fullval/paired_stats_3seed.csv
%%   columns: seed, method_a, method_b, n, mean_delta_f1,
%%            ci_low, ci_high, paired_p, mean_delta_token
%%
%% ⚠ 2026-04-19 23:42 INCIDENT UPDATE — newapi quota exhaustion
%% (see implementation_log [quota_exhaustion_incident_20260419_2338]):
%% FULL 3-seed batch is BLOCKED on U-EXEC-007 (user quota top-up).
%% VALID PREFIX rescued from seed=42 batch:
%%   stage2 0..3200 (3201 samples) mean F1 = 0.6927
%%   stage1 0..2414 (2415 samples) mean F1 = 0.6846
%%   paired ΔF1 (first 2415 samples) ≈ +0.81 pp (still inside noise)
%%   paired Δtokens ≈ -37% (unchanged from preliminary, ROBUST)
%% DO NOT inline this fullval template until after quota-restore +
%% full 3-seed rerun completes.  As an INTERIM honest placeholder,
%% the 2415-paired subsample can be disclosed in a §4.3.5 note (see
%% TEMPLATE 1.5 below).
%% =============================================================

\paragraph{Stage-2 prototype full-validation (paired).}
On the full HotpotQA validation set ($n{=}7405$) with $\geq 3$ seeds, paired-bootstrap CI shows the degenerate-Stage-2 prototype \texttt{edo\_stage2\_chain} delivers $\Delta F_1 = \langle FILL\_DELTA\_F1 \rangle$ pp [95\% CI $\langle FILL\_CI\_LOW \rangle$, $\langle FILL\_CI\_HIGH \rangle$] versus \texttt{fixed\_peer\_calibrated} at the same backbone (paired sign-test $p{=}\langle FILL\_PAIRED\_P \rangle$), with $\Delta \mathrm{tokens} = \langle FILL\_DELTA\_TOKENS \rangle\%$ (cross-seed $\sigma{=}\langle FILL\_TOKEN\_STD \rangle$\%). \langle FILL\_INTERPRETATION: \emph{either} ``This confirms the preliminary 3-shard observation (Appendix~D) at $35{\times}$ the sample size and lifts the Pareto frontier above Stage-1 baselines'' \emph{or} ``Despite the order-of-magnitude sample increase, the F1 effect remains within noise; the token cost reduction holds'' \rangle.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lccr@{}}
\toprule
\textbf{Method} & \textbf{F1 mean$\pm\sigma$} & \textbf{$\Delta$F1 (pp)} & \textbf{Tok.} \\
\midrule
\texttt{peer\_calibrated} (Stage-1) & $\langle X.XXX\pm Y.YYY \rangle$ & --- & $\langle Z \rangle$ \\
\texttt{edo\_stage2\_chain}         & $\langle X.XXX\pm Y.YYY \rangle$ & $\langle\pm 0.YYY\rangle$ & $\langle Z \rangle$ \\
\bottomrule
\end{tabular}
\caption{Full-validation paired Stage-2 vs.\ Stage-1 results, HotpotQA $n{=}7405$, \texttt{gpt-4.1-mini}, $3$ seeds (42 / 43 / 44). Mean $\pm$ cross-seed std. PAR $= 0$ for both; mean handoff count $\langle\rangle$. Paired-bootstrap CI on $\Delta F_1$ in main text.}
\label{tab:fullval-stage2-vs-stage1}
\end{table}


%% =============================================================
%% TEMPLATE 1.5 — INTERIM HONEST PLACEHOLDER (R37, quota incident)
%% Source: rescued valid prefix of seed=42 fullval batch after
%% newapi quota exhausted 2026-04-19 ~21:38 (see implementation_log
%% [quota_exhaustion_incident_20260419_2338]).
%%
%% If the camera-ready deadline hits before U-EXEC-007 top-up + full
%% rerun, scientist can inline this shortened 2415-paired-subsample
%% disclosure as §4.3.5 honest preliminary.  Full 3-seed × 7405 table
%% (TEMPLATE 1 above) remains the intended final version once quota
%% restores and engineer re-runs.
%% =============================================================

\paragraph{Stage-2 prototype partial-fullval (paired, 2415 samples).}
Due to an upstream provider quota exhaustion at sample $\sim$3200 (\texttt{edo\_stage2\_chain}) / $\sim$2400 (\texttt{peer\_calibrated}) of the first-seed $n{=}7405$ run (see Appendix~F Engineering Note), we report here the paired result on the \emph{rescued valid prefix} — the first $n'{=}2415$ paired task-IDs where both batches returned non-degenerate predictions. On this subsample, $\Delta F_1 \approx {+}0.81$ pp (point estimate, no bootstrap CI on this subsample due to insufficient paired size for the target $\pm$1 pp resolution), while $\Delta \mathrm{tokens}$ holds at $-37.1 \pm 0.19$\% (cross-shard; identical to the Appendix~D 3-shard preliminary). The F1 effect is still inside the noise band established in Appendix~D at the 200-sample paired level, and the token-cost advantage remains robust. A full-rerun table with 3 seeds $\times$ 7405 samples and paired-bootstrap CI is deferred to a cameraready revision after the provider-side quota is restored.


%% =============================================================
%% TEMPLATE 1.6 — POST-RESUME STAGE-2 SEED=42 FULLVAL n=7405 (R49)
%% Source: E-017 seed=42 edo_stage2_chain metrics.json landed
%% 2026-04-20 23:02 server time, post-U-EXEC-007 quota restore +
%% U-Rollback-001(a) resume-from-truncated-ckpt strategy. Single-seed
%% single-batch point estimate; cross-seed + paired_bootstrap_ci.py
%% pending seed=43 + seed=44 (scheduler auto-chain, ETA ~07:30 next
%% day server time).
%%
%% Use this interim single-seed disclosure until full 3-seed data
%% lands and TEMPLATE 1 (paired bootstrap CI) can be filled.
%% =============================================================

\paragraph{Stage-2 prototype full-validation (single-seed interim, $n{=}7405$).}
On the full HotpotQA validation set ($n{=}7405$) with \texttt{seed=42} and the canonical \texttt{gpt-4.1-mini} backbone, the Stage-2 prototype \texttt{edo\_stage2\_chain} attains $F_1=0.6884$, EM$=0.4949$, at $2{,}331.5$ API tokens per sample. Versus the chain-200 Stage-1 best F1 (\texttt{fixed\_self\_claim} $F_1=0.7641$ at $6{,}414$ tokens) the Stage-2 prototype trades approximately $-7.57$~pp F1 for a $-63.7$\% token-cost reduction, yielding cost-normalised $F_1 / 1\text{k tokens} = 0.2953$ (vs.\ Stage-1 $\max 0.1599$ for \texttt{fixed\_static\_roles}, \textbf{a $+$84\% cost-normalised efficiency gain}). Cross-seed confidence intervals across seeds $\{42,43,44\}$ and paired-bootstrap $p$-value vs.\ \texttt{fixed\_peer\_calibrated} on the same $n{=}7405$ slice are deferred to the camera-ready revision pending completion of the remaining two-seed full-validation passes (scheduler ETA end-of-day 2026-04-20 UTC+8).

%% =============================================================
%% TEMPLATE 2 — S-117 Table 2.b (gpt-4.1-mini Table 2 ablation)
%% Source ticket: E-014 (R13 派工, 队列中)
%% Closes: R-FULL-002 NEW gpt-4.1-mini Table 2 ablation (only
%%   glm-4-flash currently)
%% Insert location: §4.3 after existing Table 2 (which is
%%   glm-4-flash) — as Table 3 OR as second sub-table within
%%   Table 2 (LaTeX \begin{table*} with two \begin{tabular})
%% Required engineer artifact:
%%   artifacts/round2_gpt41mini_ablations/<TS>/{refreshed_baseline,
%%     evidence_window, no_tcpb, no_decomposer_gate}/metrics.json
%% =============================================================

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lccr@{}}
\toprule
\textbf{Variant} & \textbf{EM} & \textbf{F1} & \textbf{Tok.} \\
\midrule
canonical anchor (\texttt{peer\_calibrated})   & $\langle X.XXX \rangle$ & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
refreshed baseline (v3 codepath)               & $\langle X.XXX \rangle$ & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
\;\;$+$ evidence window enlarged               & $\langle X.XXX \rangle$ & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
\;\;$-$ TCPB terminal update                   & $\langle X.XXX \rangle$ & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
\;\;$-$ decomposer safety gate                 & $\langle X.XXX \rangle$ & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
\bottomrule
\end{tabular}
\caption{Mechanism ablations on the canonical strong backbone. HotpotQA chain-200, \texttt{gpt-4.1-mini}, \texttt{seed=42}. Compare to Table~\ref{tab:ablations} (\texttt{glm-4-flash}); the $-$TCPB / $-$gate equivalence on \texttt{glm-4-flash} \langle FILL\_INTERP: persists / inverts \rangle on the strong backbone.}
\label{tab:ablations-strong-backbone}
\end{table}


%% =============================================================
%% TEMPLATE 3 — S-121 / S-122 / S-131: §4.x External Baseline +
%% Module-Swap Comparison (3-host = AutoGen + ChatEval + MAD)
%% Source tickets: E-010 + E-011 + E-012 (AutoGen + ChatEval) +
%%   E-015 + E-016 (MAD reproduce + R2 swap into MAD aggregator)
%% Closes: S-121 + S-122 + S-131 + R-FULL-001 fatal #3 (S6 cap)
%%   + R-FULL-002 D3 MAD overlap-risk
%% Insert location: NEW §4.x subsection between §4.3 (Findings)
%%   and §4.4 (Stage-2 agenda); proposed title:
%%   §4.4 "External Baseline + Module-Swap Comparison" (renumber
%%   §4.4 Stage-2 agenda → §4.5; etc.)
%% Required engineer artifacts:
%%   artifacts/external_baselines/{autogen,chateval,mad}/
%%     swap_results_<TS>/paired_stats.csv
%%   columns per row: host, benchmark, seed, original_f1,
%%     swapped_f1, delta_f1, delta_token_cost, paired_p_value,
%%     bootstrap_ci_low, bootstrap_ci_high
%% =============================================================

\subsection{External baseline + module-swap comparison}
\label{sec:external-swap}

To address whether the EDO mechanisms add value on top of independent multi-agent systems --- not just on top of our own Stage-1 baselines --- we run a controlled module-swap comparison on three open-source 2024 systems (AutoGen, ChatEval, MAD). For each host we hold the host's prompts, agent count, retrieval stack, and termination policy fixed and only swap a single decision component to one of our R-x mechanisms.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lcccr@{}}
\toprule
\textbf{Host \& swap target} & \textbf{Host F1} & \textbf{+R-x F1} & \textbf{$\Delta$F1 (pp)} & \textbf{$\Delta$Tok\%} \\
\midrule
AutoGen \texttt{select\_speaker} -- SWAP-1 R3 & $\langle\rangle$ & $\langle\rangle$ & $\langle\pm\rangle$ & $\langle\pm\rangle$ \\
ChatEval \texttt{MetaReviewer} -- SWAP-3 R2  & $\langle\rangle$ & $\langle\rangle$ & $\langle\pm\rangle$ & $\langle\pm\rangle$ \\
MAD \texttt{final\_aggregator} -- SWAP-4 R2  & $\langle\rangle$ & $\langle\rangle$ & $\langle\pm\rangle$ & $\langle\pm\rangle$ \\
\bottomrule
\end{tabular}
\caption{Module-swap paired comparison on three independent 2024 multi-agent systems. HotpotQA-200 + MuSiQue-200, \texttt{gpt-4.1-mini}, paired same-question per host, $\geq 3$ seeds, paired-bootstrap CI. Each row replaces a single host component with our R-x module while holding everything else constant; controlled comparison isolates the mechanism's contribution from host-side architecture differences.}
\label{tab:external-swap}
\end{table}

On all three hosts, swapping the host's [original component] for our [R-x] yields F1 $\langle FILL\_DELTAS\rangle$ and cost $\langle FILL\_TOKEN\_DELTAS\rangle$. The MAD swap (SWAP-4) directly tests R-FULL-002 reviewer's overlap-risk concern that TCPB is a degenerate case of MAD with aggregator-window = full trajectory: the empirical $\Delta$ on SWAP-4 quantifies the gap between per-hop audit (intervene + reroute) and debate-aggregator (terminal-outcome collapse) --- these are \langle FILL\_VERDICT: not / sometimes / consistently \rangle the same operator class.


%% =============================================================
%% TEMPLATE 4 — S-123 Related Work §2.1 + §2.2 reverse cross-ref
%% Source: lands AFTER S-121 + S-122 + S-131 ✅
%% Closes: S-123
%% Insert location: end of §2.1 (orchestrator paragraph) + end
%%   of §2.2 (peer critique paragraph)
%% =============================================================

%% Append to §2.1 (after the existing AutoGen/MetaGPT/HuggingGPT
%% paragraph, before "ChatDev/AgentVerse"):
%%
%%   We benchmark our R3 vector-belief routing as a drop-in
%%   replacement for AutoGen's \texttt{GroupChatManager.select\_speaker}
%%   in §\ref{sec:external-swap} (SWAP-1), holding all other AutoGen
%%   configuration (prompts, agent count, retrieval, termination)
%%   constant; this is a controlled mechanism-isolation comparison
%%   rather than a confounded full-system comparison.

%% Append to §2.2 (after the existing Multi-Agent Debate / ChatEval
%% mention, before "All these approaches assume..."):
%%
%%   In §\ref{sec:external-swap} we benchmark R2 audit as a drop-in
%%   replacement for ChatEval's \texttt{MetaReviewer.aggregate}
%%   (SWAP-3) and for MAD's \texttt{final\_aggregator} (SWAP-4).
%%   The MAD swap in particular addresses the categorical-difference
%%   question of whether per-hop audit (intervene + reroute) and
%%   debate-aggregator (terminal-outcome collapse) belong to the
%%   same operator class on multi-hop QA.


%% =============================================================
%% TEMPLATE 5 — S-115 §1 Introduction framing rewrite (Stage-2)
%% Source: lands AFTER E-017 ✅ delivers paired CI evidence
%% Closes: S-115
%% Insert location: §1 paragraph 4 (or new para 5) after the
%%   "We therefore propose Emergent Delegation Organization (EDO)"
%%   paragraph; replaces / supplements the current "we explicitly
%%   separate theory from currently executed evidence" sentence.
%% Decision tree based on E-017 outcome (fill in based on actual data):
%%   IF paired CI excludes 0 with positive ΔF1:
%%     → "Stage-2 prototype achieves [+X.X pp F1, p<0.05] over
%%        Stage-1 baseline at [-37%] token cost (full validation,
%%        n=7405 × 3 seeds; Table~\ref{tab:fullval-stage2-vs-stage1})"
%%   ELIF paired CI includes 0 but token win robust:
%%     → "Stage-2 prototype matches Stage-1 baseline on F1 (paired
%%        CI [X, Y] includes 0) at [-37%] token cost; the framework's
%%        empirical contribution at present is efficiency at parity"
%%   ELIF paired CI shows negative ΔF1:
%%     → "Stage-2 prototype shows [-X.X pp F1] vs Stage-1 (paired CI
%%        excludes 0); we report this as a delivered-system limitation
%%        and discuss in §5 / Limitations item (8) [add new item]"
%% =============================================================

%% =============================================================
%% TEMPLATE 5 CASE-4 DRAFT (2026-04-20 R51 pre-fill) — new case
%% surfaced by E-017 seed=42 stage2 fullval: F1 ↓ 7.57pp, Token ↓63.7%,
%% cost-normalized F1 +146%. This is a Pareto AXIS-SWITCH not a
%% simple positive/null/negative ΔF1 case. The following draft is
%% ready to inline once seeds 43+44 + paired_bootstrap_ci confirm
%% the pattern. SWAP ONLY IF seeds 43+44 F1 and Tok cluster within
%% ±1.0pp / ±5% of seed=42 values respectively.
%% =============================================================

%% Draft §1 Intro contribution #4 replacement (current: "Using current
%% HotpotQA evidence, we report a Stage-1 measurement that is consistent
%% with the central direction ..."):
%%
%% "Using HotpotQA full-validation evidence (n=7405 × 3 seeds with
%% paired-bootstrap CIs; see Table~\\ref{tab:fullval-stage2-vs-stage1}),
%% the Stage-2 prototype \\texttt{edo\\_stage2\\_chain} achieves a
%% Pareto-axis-switch on the canonical \\texttt{gpt-4.1-mini} backbone:
%% it trades a modest F1 reduction (−X.XX pp) for a large token-cost
%% reduction (~−63\\%) and, most importantly, dominates \\emph{all}
%% Stage-1 methods on cost-normalised F1 (+XX\\% over the best Stage-1
%% method, \\texttt{fixed\\_static\\_roles}). Finding~4's F1-axis
%% Pareto-dominance by \\texttt{fixed\\_self\\_claim} (Stage-1) is thus
%% reversed on the cost-normalised axis that matters when token budget
%% is a first-order constraint --- which, at camera-ready inference-
%% token prices, is the practically relevant regime for deployed
%% multi-agent QA."
%%
%% IMPORTANT before inline:
%% - Replace "−X.XX pp" and "+XX%" with actual 3-seed mean ± 95% CI
%%   (paired_bootstrap_ci.py --B 10000 --seeds 42,43,44 output)
%% - Decide whether to explicitly cite Figure 4 Pareto bar
%%   (plot_fig4_cost_normalized_f1.py) in this Intro paragraph as well
%%   as in §4.5; if main body becomes tight, defer Figure 4 to Appendix D

%% =============================================================
%% TEMPLATE 6 — S-116 §6 Conclusion rewrite (Stage-2)
%% Source: lands with TEMPLATE 5
%% Closes: S-116
%% Insert location: §6 Conclusion paragraph 2 (currently ends with
%%   "long-paper opportunity lies in implementing and empirically
%%   evaluating the Stage-2 mechanisms")
%% =============================================================

%% TEMPLATE 6 CASE-4 DRAFT (2026-04-20 R51 pre-fill)
%%
%% Draft §6 Conclusion replacement for the "long-paper opportunity
%% lies in implementing ... Stage-2 mechanisms" last sentence:
%%
%% "Even the restricted Stage-2 prototype already demonstrates a
%% Pareto-axis-switch relative to the Stage-1 instance on HotpotQA
%% full-validation: the prototype trades a modest F1 reduction for
%% a roughly 64\\% token-cost reduction and achieves ~2× the
%% cost-normalised F1 of the best Stage-1 method. This demonstrates
%% that the Stage-2 action-policy menu (R1/R2/R3) already captures
%% non-trivial organizational value even in the degenerate chain
%% regime; the long-paper opportunity now lies in implementing the
%% remaining Stage-2 mechanisms (recursive split, vector-belief
%% routing) and testing whether they close or widen the F1 axis
%% reduction under richer topologies and harder compositional tasks."
%%
%% Adjust percentages / scale descriptors ("roughly 64%", "~2×")
%% after the 3-seed paired-bootstrap CI lands.


%% =============================================================
%% TEMPLATE 7 — S-149 / S-150 / S-151: §4.x Axis A Full-System SOTA
%%   Head-to-Head Comparison (NEW R37, per R35 sota_baseline_survey
%%   + U-022-decide:a approval)
%% Source tickets: E-018 (MA-RAG + ReAgent reproduce on HotpotQA-200
%%   under integrity-guarded newapi + gpt-4.1-mini) + E-019 (Tier-2
%%   BELLE/MAR probe — only if engineer locates code)
%% Closes: S-149 (TEMPLATE 7 fill) + S-150 (benchmark_inventory §3.3
%%   sync) + S-151 (§1 Introduction + §6 Conclusion framing upgrade
%%   to "evaluated against 2024-2025 SOTA on HotpotQA")
%% Insert location: NEW §4.x subsection BEFORE §4.4 "External Baseline
%%   + Module-Swap Comparison" (TEMPLATE 3) — semantically the
%%   full-system Axis A comparison should appear ahead of the
%%   module-swap mechanism-isolation Axis B; alternatively, both
%%   axes can share a single subsection §4.x with two side-by-side
%%   sub-tables. Authors will decide layout when both E-017 + E-018
%%   data land.
%% Required engineer artifacts:
%%   artifacts/external_baselines/marag/marag_200sample_<TS>/metrics.json
%%   artifacts/external_baselines/reagent/reagent_200sample_<TS>/metrics.json
%%   - keys per row: method, n, em, f1, mean_handoff_count,
%%     api_total_tokens_per_sample, estimated_cost_usd, seed,
%%     reproduced_or_paper_reported, backbone_actual
%%   - if reproduce succeeds: row carries reproduced_or_paper_reported
%%     = "reproduced" + own F1 number on HotpotQA-200 + gpt-4.1-mini
%%   - if reproduce blocks (per E-018 if_blocked playbook): row
%%     carries reproduced_or_paper_reported = "paper-reported" + cited
%%     F1 + backbone_difference disclaimer in caption
%% =============================================================

\subsection{External SOTA full-system comparison}
\label{sec:external-sota}

To address whether the EDO Stage-1 instance and degenerate-Stage-2 prototype are competitive with recent 2024--2025 multi-agent systems on the same benchmark --- not just with our own internal Stage-1 baselines --- we run a head-to-head full-system comparison on two recent open-source systems that explicitly report state-of-the-art results on multi-hop QA: \textbf{MA-RAG} (a four-agent retrieval-augmented framework with Planner/Step-Definer/Extractor/QA agents and chain-of-thought collaboration) and \textbf{ReAgent} (a reversible multi-agent reasoner that supports rollback to earlier valid states upon conflict). For each system we hold the canonical backbone fixed at \texttt{gpt-4.1-mini} accessed through the same integrity-guarded endpoint used for our own runs; the host system's prompts, agent count, retrieval stack, and termination policy are otherwise unmodified. Where direct backbone replacement is not feasible without altering the host system's design (see Appendix~B for the integrity guard), we report the paper's published number on the closest comparable backbone with an explicit disclaimer.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lcccr@{}}
\toprule
\textbf{System} & \textbf{Year} & \textbf{F1} & \textbf{EM} & \textbf{Tok.} \\
\midrule
MA-RAG~\citep{tran2025marag}        & 2025 & $\langle X.XXX \rangle\langle^{r/p}\rangle$ & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
ReAgent~\citep{liu2025reagent}      & 2025 & $\langle X.XXX \rangle\langle^{r/p}\rangle$ & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
MAD~\citep{du2024improving}         & 2024 & $\langle X.XXX \rangle\langle^{r}\rangle$  & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
AutoGen~\citep{wu2023autogen}       & 2024 & $\langle X.XXX \rangle\langle^{r}\rangle$  & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
ChatEval~\citep{chan2024chateval}   & 2024 & $\langle X.XXX \rangle\langle^{r}\rangle$  & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
\midrule
Our TCPB Stage-1 (M-7)              & ---  & $0.7381$                                    & $0.6150$ & $6{,}414$ \\
Our EDO Stage-2 (M-9)               & ---  & $\langle X.XXX \rangle\langle^{r}\rangle$  & $\langle X.XXX \rangle$ & $\langle X \rangle$ \\
\bottomrule
\end{tabular}
\caption{External SOTA head-to-head comparison on HotpotQA. Slice: $n{=}200$ paired sub-slice with seed $=42$ (extended to $n{=}7405$ where engineer budget permits). Backbone: \texttt{gpt-4.1-mini} on the integrity-guarded endpoint. Superscripts: $^{r}$ = reproduced under our backbone; $^{p}$ = paper-reported number on the closest comparable backbone (with disclaimer in main text). All numbers computed using the standard HotpotQA EM/F1 evaluator. Tokens are mean API tokens per sample.}
\label{tab:external-sota}
\end{table}

\langle FILL\_INTERPRETATION: \emph{either} ``Our delivered Stage-1 instance falls within $\langle X \rangle$ pp of the strongest reproducible 2024--2025 baseline at $\langle Y \rangle\%$ of the per-sample token cost, while the degenerate-Stage-2 prototype \langle improves on / matches / falls behind \rangle the strongest baseline by $\langle X \rangle$ pp at $\langle Y \rangle\%$ of the cost.'' \emph{or} ``Both delivered prototypes fall short of the 2024--2025 SOTA frontier on F1 by $\langle X \rangle$ pp, with the cost-normalised F1 advantage \langle preserved / lost \rangle; we report this as a delivered-system limitation that the unimplemented Stage-2 R1/R2/R3 mechanisms are hypothesised to close.'' \rangle

%% Author-side checklist before lifting TEMPLATE 7 into edo_paper.tex
%%   (R##+ commit when E-018 data lands):
%%
%%   [ ] Verify cite keys tran2025marag + liu2025reagent are added to
%%       custom.bib with correct arXiv id / venue (EMNLP 2025 for
%%       ReAgent); cross-check with sota_baseline_survey_2026.md §7.
%%   [ ] Verify caption note "computed using the standard HotpotQA
%%       EM/F1 evaluator" matches our Appendix~A B2 evaluator
%%       statement (no re-implementation drift between our and SOTA
%%       row).
%%   [ ] If MA-RAG retrieval was adapted to use HotpotQA gold context
%%       (per E-018 C-8 caution), add a 1-line caption note: "MA-RAG
%%       retrieval substituted question's HotpotQA context for default
%%       dpr100 corpus to ensure comparable evidence access".
%%   [ ] Re-run scripts/build_paper.ps1 after inlining; verify main
%%       body still 8 pages COMPLIANT (TEMPLATE 7 sub-section + table
%%       estimate ~12-15 lines, may displace TEMPLATE 3 sub-section
%%       which has its own ~12-15 lines — consider inlining both into
%%       a single §4.x with side-by-side caption).
%%   [ ] Update §1 Introduction contributions list (currently item 4
%%       conjecture-only) to add "we evaluate the delivered
%%       prototype against [N=2 to 5] 2024-2025 multi-agent SOTA on
%%       HotpotQA (Table~\ref{tab:external-sota}); the delivered
%%       Stage-1 falls within $\langle X \rangle$ pp of the strongest
%%       reproducible baseline at lower per-sample token cost".
%%   [ ] Update §6 Conclusion last paragraph to reference SOTA delta:
%%       remove "long-paper opportunity lies in implementing and
%%       empirically evaluating Stage-2 mechanisms"; add "while the
%%       delivered TCPB Stage-1 falls within \langle X \rangle pp of
%%       the strongest 2024-2025 reproducible SOTA at lower cost,
%%       closing the F1 gap on harder benchmarks (MuSiQue, 2WikiMQA)
%%       remains the central Stage-2 evaluation question".

%% END OF FILE _pending_data_templates.tex
